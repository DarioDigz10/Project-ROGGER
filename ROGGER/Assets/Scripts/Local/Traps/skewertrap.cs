using UnityEngine;

public class skewertrap : MonoBehaviour
{
    /*Player player;
    [SerializeField] float damage;
    [SerializeField] float trampaDesactivada;
    private bool activada=false;
    private float contador = 0;

    void Start()
    {
        player = GameManager.Instance.Player;
    }

    private void Update()
    {
        if (activada == true && contador < trampaDesactivada)
        {
            contador = contador + Time.deltaTime;
        }
        else
        {
            activada = false;
            contador = 0;
        }
    }

    private void OnTriggerEnter(Collider player)
    {
        playerSetup ps = player.GetComponent<playerSetup>();
        if (player.CompareTag("Player") && activada==false) {
            ps.actualHealth -=damage;
            activada = true;
        }
    }

    */
}
